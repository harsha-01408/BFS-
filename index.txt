Shortest Path with Single-Use Teleports

You are given an m x n grid of characters grid where:
- '.' represents an empty cell,
- '#' represents an obstacle,
- 'A' to 'Z' represent teleport portals.

You start at the top-left cell (0, 0) and want to reach the bottom-right cell (m - 1, n - 1).

In one move, you may move to an adjacent cell (up, down, left, right) if it is within bounds and not an obstacle.

Teleport rule:
- If you step onto a portal cell containing letter X, you may instantly teleport to any other cell that also contains letter X.
- Teleporting costs 0 moves.
- Each portal letter (A–Z) can be used for teleporting at most once during the entire path.

Return the minimum number of moves required to reach (m - 1, n - 1).
If it is impossible, return -1.

Constraints:
- 1 <= m, n <= 100
- grid[i][j] is '.', '#', or an uppercase English letter ('A' to 'Z')
- grid[0][0] != '#'
- grid[m - 1][n - 1] != '#'
- 0 <= number of distinct portal letters in grid <= 15


xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

Cost optimization

You are given a square grid Dir of size N × N. Every cell contains one uppercase
direction letter from {L, R, U, D}. From any cell, you may move to one of its
four neighboring cells (up, down, left, right) as long as you stay inside the
grid.

Cost of a move:
• If Dir[i][j] = L, then moving left from (i, j) to (i, j−1) costs 0; moving in
  any other valid direction costs 1.
• If Dir[i][j] = R, then moving right from (i, j) to (i, j+1) costs 0; moving in
  any other valid direction costs 1.
• If Dir[i][j] = U, then moving up from (i, j) to (i−1, j) costs 0; moving in
  any other valid direction costs 1.
• If Dir[i][j] = D, then moving down from (i, j) to (i+1, j) costs 0; moving in
  any other valid direction costs 1.

You are also given a start cell (Sx, Sy) and a destination cell (Dx, Dy). Your
task is to compute the minimum total cost needed to travel from the start cell
to the destination cell.

Input format:
• The first line contains an integer N, the dimension of the grid.
• The next N lines each contain N space-separated characters (L/R/U/D) forming
  the Dir matrix.
• The next line contains two space-separated integers Sx Sy (start cell).
• The next line contains two space-separated integers Dx Dy (destination cell).

Output format:
• Print one integer: the minimum cost to reach (Dx, Dy) from (Sx, Sy).

Constraints:
• 1 ≤ N ≤ 3 × 10^3
• 1 ≤ Sx, Sy, Dx, Dy ≤ N

xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

Problem Description
A network of m roads connects N cities (numbered from 1 to N). There may be more than one road connecting one city with another. Some of the roads are paid. There are two ways to pay for travel on a paid road i from city ai to city bi:

1) Pay in advance, in a city ci (which may or may not be the same as ai).
2) Pay after the travel, in the city bi.

The payment is Pi in the first case and Ri in the second case.

Rule:
When you travel along road i (ai -> bi):
- If your route has visited city ci at some point BEFORE taking this road, you may pay Pi.
- Otherwise, you must pay Ri.

Task:
Find the minimum possible total cost to travel from city 1 to city N.
If it is impossible to reach city N from city 1, output:
impossible

Input Format
- The first line contains two integers N and m.
- Each of the next m lines contains five integers:
  ai bi ci Pi Ri

Output Format
- Print the minimum total cost from city 1 to city N.
- If no valid trip exists, print:
  impossible

xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

You are given an undirected graph with N nodes and M edges.
All edges have weight 1.

There is a special node V. Any edge that touches V has weight 0.
All other edges keep weight 1.

Given a source node S, compute the shortest distance from S to every node.

Output an array dist of length N:
- dist[i] is the shortest distance from S to node (i+1)
- if node (i+1) is unreachable, dist[i] = -1

xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

You have g_nodes Uber hubs labeled 1..g_nodes. The network is a complete undirected graph,
meaning you can connect every pair of hubs.

Some connections require a paid infrastructure upgrade (cost = 1). These paid connections are
given as g_edges pairs: (g_from[i], g_to[i]). Every other possible pair of hubs can be connected
for free (cost = 0).

Your task is to choose a set of connections so that all hubs become connected (there is a path
between every pair of hubs), while minimizing the total upgrade cost.

Return the minimum total cost. If the network is already connected using only free connections,
the answer is 0.

xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

You are given a string target and two positive integers x and y.

Start with a value cur = 0.
In one operation, you may do either of the following:
- set cur = cur + x
- set cur = cur + y

Each time you reach a new value, append its decimal representation to a string s.

Your task is to generate a string s such that target is a subsequence of s.

Return the smallest possible valid string s.
Among all valid strings:
1. choose the one with minimum length
2. if multiple strings have the same length, return the lexicographically smallest one

If it is impossible, return "-1".

A string a is a subsequence of string b if a can be obtained by deleting some characters from b without changing the order of the remaining characters.

Example 1:
Input: target = "27", x = 2, y = 3
Output: "247"

Explanation:
One possible sequence of values is:
0 -> 2 -> 4 -> 7

This generates the string "247", and "27" is a subsequence of "247".

Another possible sequence is:
0 -> 2 -> 5 -> 7

This generates the string "257", and "27" is also a subsequence of "257".

Both strings are valid and have the same length, but "247" is lexicographically smaller, so the answer is "247".

Example 2:
Input: target = "2710", x = 2, y = 3
Output: "24710"

Explanation:
A valid sequence is:
0 -> 2 -> 4 -> 7 -> 10

This generates the string "24710", and "2710" is a subsequence of it.

Constraints:
- 1 <= target.length <= 100
- target consists only of digits
- 1 <= x, y <= 10^9

xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

You are given an n x m grid maze where:

- maze[i][j] = 0 means the cell is empty
- maze[i][j] = 1 means the cell is blocked

You start at the top-left cell (0, 0) and want to reach the bottom-right cell (n - 1, m - 1).

You are also given an integer k.

In one move, from a cell (i, j), you may move in exactly one of the four directions:

- up
- down
- left
- right

and travel any distance from 1 to k cells in that direction.

A move is valid only if:

- every cell passed through is inside the grid
- every cell passed through is empty
- the destination cell is also empty

You are not allowed to jump over blocked cells.

Return the minimum number of moves needed to reach (n - 1, m - 1).

If it is impossible to reach the destination, return -1.

Example 1:
Input: maze = [[0,0,0],[1,0,0]], k = 5
Output: 2
Explanation:
One optimal path is:
(0,0) -> (0,2) -> (1,2)

Example 2:
Input: maze = [[0,1],[0,0]], k = 1
Output: 2
Explanation:
One optimal path is:
(0,0) -> (1,0) -> (1,1)

Constraints:
- 1 <= n, m <= 100
- 1 <= k <= 100
- maze[i][j] is either 0 or 1

xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
